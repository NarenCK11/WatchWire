# Add project specific ProGuard rules here.
# minifyEnabled is off for this MVP build type, so this file is currently unused, but is
# kept so enabling R8 later doesn't require re-adding the reference in build.gradle.kts.
